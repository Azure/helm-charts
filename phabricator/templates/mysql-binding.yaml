apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceBinding
metadata:
  name: {{ template "phabricator.fullname" . }}-mysql-binding
  labels:
    app: {{ template "phabricator.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  instanceRef:
    name: {{ template "phabricator.fullname" . }}-mysql-instance
  secretName: {{ template "phabricator.fullname" . }}-mysql-secret
