{{- if not .Values.mysql.embeddedMaria }}
apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceBinding
metadata:
  name: {{ template "mediawiki.fullname" . }}-mysql-binding
  labels:
    app: {{ template "mediawiki.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  instanceRef:
    name: {{ template "mediawiki.fullname" . }}-mysql-instance
  secretName: {{ template "mediawiki.fullname" . }}-mysql-secret
{{- end }}
