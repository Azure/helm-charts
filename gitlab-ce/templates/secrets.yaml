apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  redis-password: {{ b64enc "secret" }}
  gitlab-root-password: {{ b64enc "secret" }}
  dbHost: {{ b64enc "secret" }}
  dbUser: {{ b64enc "secret" }}
  dbPassword: {{ b64enc "secret" }}
  host: {{ b64enc "secret" }}
  password: {{ b64enc "secret" }}
